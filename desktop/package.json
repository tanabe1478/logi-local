{
  "name": "logi-local-desktop",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && electron .",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "npm run build && node tests/ui-smoke.mjs",
    "test:renderer": "npm run build && node tests/renderer-smoke.mjs",
    "test:lifecycle": "npm run build && node tests/lifecycle-smoke.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@sinclair/typebox": "0.34.48",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "electron": "44.3.0",
    "vite": "8.3.0"
  }
}
